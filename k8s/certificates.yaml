apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: otfl-tls
  namespace: otfl
spec:
  secretName: otfl-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - otfl.inevitable.fyi
