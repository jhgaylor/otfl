apiVersion: v1
kind: Service
metadata:
  name: otfl
  namespace: otfl
  labels:
    app: otfl
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "otfl"
spec:
  type: ClusterIP
  selector:
    app: otfl
  ports:
    - name: http
      port: 80
      targetPort: http
